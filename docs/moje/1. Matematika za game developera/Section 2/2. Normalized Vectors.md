
As we know, Points  represent locations in space, and Vectors represent direction and magnitude. Both represent numerical value but different concepts.

What is a "normalized vector", or how do we normalize vector
- Making his magnitude or length being 1, by increase or decrease
- But preserving it's direction and sense

This is something that pops up all the time in gme development especially in math
- Position updates using spped + direction
- Physics simulations + collision detection
- Shaders, especially lighting
Mostly ensuring that callculations fall in a specific range

# Proportion is the way to calculate points of normalized vector

Idea is like this when I think about it: distance of current vector divided by it's points should be the same as distance of normalized vector (1) divided by points we want to find 

We represent normalized vector as v eith a hat
$$\hat{v}$$

This is the official equation: normalized vector equals vector devided by magnitude
$$
 \hat{v} = \frac{\vec{v}}{\|\vec{v}\|}
$$

We would never have to do this by hand, except in some rare cases; because in threejs or in unity, or in shader language,, you have dedicated methods for calculating unit vector, and they are called `normalize` or similar ``

# Example

But let's say we have a vector that starts at (0,0) and ends at (2,3)

![[norm.excalidraw|700]]

Same goes if we would have vector which magnitude is lower than 1; Let's say you have vector that goes from (0,0) to (0.5,0.25)

![[2026-09-21_16-34.png]]


# I assume same principle is applied on 3D

You just have one more point to calculate


# What we did in nomalization is we Scale it by inverse of length.

# Šta konkretno znači "Inverse of Length"

"Inverse" ovde znači **recipročnu vrednost (1/x)**, ne neki drugi tip inverza (nije reč o "suprotnom smeru" kao kod −v, ovo je čisto matematički pojam iz aritmetike).

## Šta to konkretno znači

Kada normalizuješ vektor, radiš ovo:

$$\hat{v} = \frac{v}{|v|}$$

To jest — **deliš** vektor sa njegovom dužinom (magnitude). Ali deljenje sa brojem x je matematički isto što i **množenje sa 1/x** (recipročnom vrednošću tog broja):

$$\frac{v}{|v|} = v \times \frac{1}{|v|}$$

Dakle "**scale it by inverse of length**" = pomnoži svaku komponentu vektora sa $\frac{1}{|v|}$, umesto da praviš deljenje.

## Zašto se to tako kaže/radi

To je pre svega **stilski/performansni** izbor u programiranju (na to sumnjam da referenca cilja, s obzirom na temu vektora — game dev/grafika):

- Deljenje (`/`) je na procesoru **sporija** operacija od množenja (`*`).
- Ako imaš vektor sa 3 komponente (x, y, z), umesto da radiš 3 deljenja:
    
    ```
    x / lengthy / lengthz / length
    ```
    
    izračunaš recipročnu vrednost **jednom**:
    
    ```
    inv = 1 / length
    ```
    
    pa onda samo množiš (3 množenja umesto 3 deljenja):
    
    ```
    x * invy * invz * inv
    ```
    

Ovo je čest pattern u kodu za normalizaciju vektora (Unity, OpenGL shader-i, itd.) — recimo:

```csharp
float invLength = 1f / vector.magnitude;
Vector3 normalized = new Vector3(x * invLength, y * invLength, z * invLength);
```

## Rezime

| Termin                         | Značenje                                                                        |
| ------------------------------ | ------------------------------------------------------------------------------- |
| **length** (magnitude)         | $\|v\|$, dužina vektora                                                         |
| **inverse of length**          | $\frac{1}{\|v\|}$ — recipročna vrednost dužine                                  |
| **scale by inverse of length** | pomnoži vektor sa $\frac{1}{\|v\|}$ (matematički identično deljenju sa $\|v\|$) |

Znači, to je samo drugačiji način da se kaže "podeli sa dužinom" — ali izraženo kroz množenje, jer se tako brže izvršava u kodu.