explain that if we have numbers fom 0 to 1 (maybe generated by `Math.random()`) and we multiply them by `2 * Math.PI` we get all the radians that go spectar of radians value for an angle that can cover entire circle of angles
```ts
Math.random() * 2 * Math.PI
```