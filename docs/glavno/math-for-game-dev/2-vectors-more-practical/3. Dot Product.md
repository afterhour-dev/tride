
Also called `Scalar Product` or `Scalar Projection`, but most people use term `Dot Product`; we are going to use `Dot Product` in this lesson but also term `Sclar Projection` because mentioned neaming explain important thing about the nature of Dot Product.

Dot product is product of  two Direction Vectors:
- It takes two direction vectors
- produces  scalar

It mesures similarity of 2 vectors; or mesures how closely they align.

# Dot Product equation

 I assume It is called dot, because we put dot (`·`) between two Direction Vectors explain this operation. 
 
$$
\begin{gather}
\vec{v}_{1} \cdot \vec{v_{2}} = |\lvert a\rvert| & |\lvert b\rvert| & \cos{\theta} 
\end{gather}
$$

Looking at equation above can also tell you that there is a two way of calculating dot product between two Direction Vectors. Look at the left side of the equation and look at the right side.

Next we will explore both ways of calculating dot product. And in that process we will also make metioned equation be more clear 


# Computing Dot Product

## `v1 · v2`  way

So we have two Direction Vectors, and:
- their dot product is scalar value
- scalar value is floating point number

$$
\begin{gather}
\vec{v}_{1} \cdot \vec{v_{2}} 
\end{gather}
$$

And this is how we calculate:
- We multiply element of one Vector with his counterpart from another vector
- We add those values together

![[ditproduct.excalidraw|700]]

## `||v1|| * ||v2|| * cos(θ)`  way

By reading next equation you can say that you can calculate Dot Product of two Direction vectors by multiplying their magnitudes (||a|| and ||b||) together and multiplying  cosinus of the angle theta between these mentioned vector (`cos(θ)`) 
$$
\begin{gather}
|\lvert a\rvert| & |\lvert b\rvert| & \cos{\theta} 
\end{gather}
$$

`||a||` is magnitude/length of `a` vector, `||b||`  is magnitude/length og `b` vector

And `θ` angle is angle between a vector and b vector

So you can also calculate dot product  if you have angel between two vectors; you can multiply cosinus of the angle and  magnitudes/lengths of both vectors.

Next it is important to know what sclar value we get from dot product calculation actually represent. 

# Scalar Projection

Well, calculation of dot product gives us a scalar that we can call `Scalar Projection`

**`Scalar Projection` is related to the cosinus of the angle between two Vectors, scaled by magnitudes of mentioned vectors.**

And it will be very easy to understand this if we observe unit vectors that are on unit circle, before we maybe observe non unit vectors.

![[scalar-projectionis.excalidraw|700]]

And since magnitude of these vectors is acutually 1, we would also have same sclar projection of v1 onto v2

But, let's use trigonometry to understand things better.