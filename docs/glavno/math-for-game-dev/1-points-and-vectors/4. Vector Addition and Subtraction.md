
We will cover addition and subtraction, both for `Position Vectors` (Points) and `Direction Vectors` (Vectors)

Calculations can combine both of Pointa and Vectors.

# Addition of multiple `Direction Vectors` is giving you new `Direction Vector`

![[2026-09-20_21-38.png]]

You got new vector (green)
- magnitude (length) of 1
- direction positive

# Addition of a Point and the Vector gives you the new point

So Addition of a `Position Vector` and the `Direction Vector` gives you the new `Position Vector`

![[additionofapointandvec.excalidraw|700]]

# Accumulating vectors

![[additionone.excalidraw|700]]
### Accumulation doesn't need to go in order

we changed order of accumaulation so we have v2 + v1 instead v1 + v2

![[additioninnoorder.excalidraw|700]]

# Subtraction between the points gets you the new vector

The rule: `B - A` gives the vector that points _from_ A _to_ B.

**Why this is consistent with point + vector = point:**

These two operations are meant to be inverses of each other. If you take the vector `v = B - A`, and add it back to A, you should land exactly on B:

$$A + (B - A) = A + v = B$$

![[difference-newvec.excalidraw|700]]

### Considering subtractions between two points, let's find central point between two points

Point 16 and point 8 for example.

We are trying to apply what we learned, and not obvious `(16 + 8)/2` calculation, so don't do that.

So we know that `B - A` gives the vector that points _from_ A _to_ B.

Also we know that adding point and vector givs us a point.

![[2026-09-20_21-41.png]]

# Subtraction between Direction Vectors

Oduzimanje vektora zbunjuje mnoge jer se čini da "nema smisla" da oduzmeš strelicu od strelice. Ključ je u tome da se **oduzimanje definiše kao sabiranje suprotnog (negiranog) vektora**.

## Osnovna ideja

$$a - b = a + (-b)$$

Gde je **−b** vektor koji ima:

- **isti magnitude** kao b
- **suprotan smer (sense)** od b

Dakle, oduzimanje nije neka posebna nova operacija — to je samo sabiranje, gde prvo "okreneš" drugi vektor za 180°, pa ga sabereš kao i obično.

## Primena na random tri vektora

Neka je:

- **v1** = +10 (direction positive, magnitude 10)
- **v2** = −5 (direction negative, magnitude 5)
- **v3** = +3 (direction positive, magnitude 3)

**v1 − v2** = v1 + (−v2)  
−v2 znači: okreni v2, magnitude ostaje 5, ali sad je positive  
= 10 + (+5) = **15**

**v1 − v3** = v1 + (−v3)  
−v3 = magnitude 3, ali sad negative  
= 10 + (−3) = **7**

**v2 − v3** = v2 + (−v3)  
= −5 + (−3) = **−8**

Hajde da ovo vizuelizujem na number line-u da bude jasnije šta se dešava geometrijski.## Šta se dešava geometrijski

1. Nacrtaš **v1** normalno, od 0 do 10.
2. Za **−v3**, kreneš od tačke gde se v1 završio (10), ali sada crtaš strelicu dužine 3 u **suprotnom smeru** (ulevo, jer je v3 bio positive, pa −v3 postaje negative).
3. Gde stigneš — to je rezultat: **10 − 3 = 7**.

![[subtractingvecfromvec.excalidraw|700]]

Dakle, geometrijski, oduzimanje se radi **isto kao sabiranje** (tip-to-tail metoda), samo se drugi vektor prvo **okrene za 180°** pre nego što ga "nastaviš" od kraja prvog.

## Nekoliko bitnih napomena

**Oduzimanje NIJE komutativno.**

Za razliku od sabiranja gde je `a + b = b + a` ; ovde je `a − b ≠ b − a` (osim ako su jednaki). Konkretno:

- v1 − v2 = 15
- v2 − v1 = −15 (isti magnitude, suprotan smer!)

To ima smisla — v1 − v2 zapravo pita "koliki je pomeraj od v2 do v1", a v2 − v1 pita suprotno pitanje, pa je logično da je odgovor obrnut.

**Oduzimanje NIJE ni asocijativno**

U smislu slobodnog premeštanja zagrada bez menjanja znakova — jer čim uvedeš oduzimanje, ti u suštini uvodiš "skrivene" negativne vektore, i tada moraš paziti koji vektor je negiran.

**Praktično pravilo koje pamtiš:**

> Da oduzmeš vektor b od a, samo okreneš **sense** (smer) vektora b (magnitude ostaje isti, pravac/direction ostaje isti — u tvom kontekstu na brojevnoj pravoj), i onda normalno sabereš.

***

1D makes things simple, but also same concept applies to 2D or 3D.