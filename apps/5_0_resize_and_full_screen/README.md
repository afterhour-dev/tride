# Resize and Full Screen

We want to fit our scene in the viewport. So we need to handle resizing on window.

Before that we need to handle css.

We also need to handle pixel ratio

And we also need to handle full screen.